# 桌面悬浮语音转文字小工具

一个常驻桌面的小悬浮球。点一下开始说话，再点一下结束，文字出现在按钮上方的面板里，右侧有「复制」按钮。识别在你自己电脑上跑，语音不上传任何服务器。

---

## 一、安装（只做一次）

### 1. 装 Python

去 python.org 下载 Python 3.10 或更新的版本。

**安装时务必勾选最下面那个 `Add Python to PATH`**，这是新手最常踩的坑，忘了勾后面所有命令都会报"不是内部或外部命令"。

### 2. 装依赖

把这个文件夹解压到任意位置，比如 `D:\语音工具\`。

在文件夹空白处按住 `Shift` + 右键 → 选「在此处打开 PowerShell 窗口」，然后粘贴：

```
pip install -r requirements.txt
```

等它跑完。大概一两分钟，取决于网速。

### 3. 第一次运行会自动下载模型

双击 `启动.bat`。

第一次启动时它会从网上下载语音识别模型（small 模型约 500MB），这时候悬浮球是灰色的，状态栏显示"正在加载模型…"。下载完成后球会变蓝，就可以用了。

**以后每次启动只需要几秒**，模型只下载一次。

---

## 二、怎么用

| 操作 | 说明 |
|---|---|
| 点击蓝色话筒球 | 开始录音，球变红 |
| 再点一次（变成方块） | 结束录音，开始识别 |
| 识别完成 | 文字出现在上方面板，并自动复制到剪贴板 |
| 「复制」按钮 | 手动复制面板里的全部文字 |
| 「清空」按钮 | 清掉面板内容，准备下一段 |
| 拖动底部黑色条 | 移动悬浮球位置 |
| `Ctrl + Alt + 空格` | 全局快捷键，任何时候都能开始/结束录音 |
| 右上角 ✕ | 退出 |

**面板里的文字是可以直接编辑的。** 识别出来有错别字，鼠标点进去改掉再复制就行，不用回到别的软件再改。

连续说几段会自动往下续行，一次性复制走。

---

## 三、想调整的地方

用记事本打开 `voice_widget.py`，最上面有个「配置区」：

```python
MODEL_SIZE = "small"      # 模型大小
LANGUAGE = "zh"           # 识别语言
AUTO_COPY = True          # 是否自动复制
HOTKEY = "ctrl+alt+space" # 全局快捷键
```

**模型大小怎么选：**

| 值 | 大小 | 速度 | 中文准确度 |
|---|---|---|---|
| `base` | ~150MB | 很快 | 一般 |
| `small` | ~500MB | 适中 | 够用（默认） |
| `medium` | ~1.5GB | 偏慢 | 好 |
| `large-v3` | ~3GB | 慢 | 最好 |

普通笔记本用 `small`。如果你的电脑配置不错、又对准确度要求高，换成 `medium`，识别一段 30 秒的话大概多等几秒。改完保存，重启程序即可（会重新下载对应模型）。

**语言设置：** `"zh"` 中文、`"en"` 英文。如果你经常中英夹杂，把它改成 `None`（不加引号），让它自动判断。

---

## 四、常见问题

**悬浮球一直是灰的，状态显示"加载失败"**
多半是模型下载被网络中断了。关掉重开会续下。如果反复失败，试试挂代理，或者先把 `MODEL_SIZE` 改成 `base` 试小的。

**状态栏显示"麦克风打不开"**
Windows 设置 → 隐私和安全性 → 麦克风，确认"允许桌面应用访问麦克风"是开启的。

**双击 .bat 一闪而过没反应**
把 `启动.bat` 里的 `pythonw` 改成 `python`，再双击，这样会保留黑窗口，能看到具体报的什么错。

**识别出来是繁体字**
脚本里已经加了 `INIT_PROMPT` 强制简体，如果偶尔还是出繁体，把那句提示词改得更明确一点，比如加上"使用简体中文"。

**想开机自启动**
`Win + R` 输入 `shell:startup` 回车，把 `启动.bat` 的快捷方式拖进去。

---

## 五、文件说明

```
voice_widget.py    主程序，所有逻辑都在这里
requirements.txt   依赖清单
启动.bat           双击运行
使用说明.md         本文件
```
